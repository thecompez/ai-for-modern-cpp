# Claude Code Instructions

Read `AGENTS.md` first.

Treat `AGENTS.md` as the canonical repository-wide instruction file for all implementation, refactoring, testing, release, and review work.

After reading it, follow its task-routing table and read each selected guide under `docs/agent/` completely before editing.
